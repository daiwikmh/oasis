// Sentry browser SDK wiring lands with spec 13 (web deploy).
// Stubbed for now so callsites compile without the dependency installed.

export function initSentry() {
  // no-op until @sentry/nextjs is wired
}
